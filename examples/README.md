# GeniusAuth Laravel SDK example

Copy `routes.php` into a Laravel application after installing `geniusauth/laravel` and configuring credentials from the [GeniusAuth documentation](https://docs.geniusauth.com/laravel).

The `/sign-in` route starts the OAuth flow. The `/account` route demonstrates the `geniusauth` middleware and returns the authenticated identity.
